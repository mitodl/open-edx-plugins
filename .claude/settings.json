{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); if [ -n \"$f\" ] && [ -f \"$f\" ]; then uv run pre-commit run --files \"$f\" 2>&1; fi; true",
            "timeout": 120,
            "statusMessage": "Running pre-commit on edited file..."
          }
        ]
      }
    ]
  }
}
