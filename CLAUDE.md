# Claude Instructions

@AGENTS.md

## Plugin-scoped context

Each plugin under `src/<plugin>/` also has its own `CLAUDE.md`. When working
inside a specific plugin directory, read that file first — it covers the
plugin's purpose, key files, entry points/settings, and gotchas that AGENTS.md
above doesn't get into on a per-plugin basis.
